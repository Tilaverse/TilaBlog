{
  "name": "astro-blog",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node ./scripts/generate-lists.js && astro build",
    "preview": "astro preview",
    "format": "prettier --write .",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "gray-matter": "^4.0.3",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
