import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://your-username.github.io/your-repo-name', // set correctly
  base: '/', // or '/your-repo-name/' if needed
});
